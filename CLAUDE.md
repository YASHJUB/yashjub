# مشروع يشجب (YASHJUB)

منصة رقمية سعودية تربط العملاء بمزودي خدمات البناء والبنية التحتية. تعمل على نظام مشابه لـ Uber (طلب فوري) + منصات الحجز (جدولة مسبقة).

- **الرابط الحالي:** `https://yashjub.onrender.com`
- **GitHub:** `https://github.com/YASHJUB/yashjub`
- **الاستضافة:** Render — يتحدّث تلقائياً عند كل `git push` (انتقل من Railway بعد انتهاء الفترة المجانية هناك، 2026-08-13). **قاعدة البيانات ما انتقلت** (SQLite محلي مستثنى من git) — الموقع الجديد بدأ بقاعدة فاضية، أي مزودين/بيانات كانت على Railway لازم تُسجَّل من جديد يدوياً.

## الخدمات المتاحة

| الخدمة | النوع | السعر |
|---|---|---|
| فان تاي واي 🚚 | فوري | العميل يحدد سعره (حد أدنى 150 ريال) |
| سطحة 🚛 | فوري | العميل يحدد سعره (حد أدنى 100 ريال) |
| حاوية 📦 | مجدول | 150–400 ريال/يوم (حد أدنى 10 أيام) |
| معدات ثقيلة 🏗️ | قريباً | — |

**هذا الجدول أصبح بيانات حقيقية بجدول `services`** (مو مكتوب بالكود) — لوحة الإدارة (قسم "الخدمات") تدير الكتالوج بالكامل (إضافة/تعديل/حذف/تفعيل)، وقائمة الخدمات بالصفحة الرئيسية (`index.html`) تُحمَّل ديناميكياً منه عبر `GET /api/services/active` (`app.js`'s `loadServices()`). راجع قسم "إدارة الخدمات" بالأسفل للتفاصيل الكاملة.

- وايت ماء وسطحة: **بدون سعر ثابت** — العميل يكتب السعر اللي يرضاه بحقل "حدد سعرك" بصفحة `order.html` (نظام مزايدة بسيط، مو تسعير منصة). رسوم الخدمة 5% تُحسب تلقائياً على السعر اللي يحدده. لوحة المزود (`provider.html`) تعرض هذا السعر بوضوح بالطلب الوارد — المزود يقبل أو يرفض بناءً عليه (`servicesData` بـ `order.js` يُبنى من `services.min_price` بدل سعر ثابت لهاتين الخدمتين).
- الحاوية: صغيرة (3م)، متوسطة (6م)، كبيرة (12م) — كل منتج حاوية (يضيفه المزود) له حجم وسعر حزمة وحد أدنى أيام خاص فيه (المزود هو اللي يحدد الحد الأدنى، مو ثابت 10 للجميع)
- السطحة: تحتاج موقعين — موقع السيارة الحالي + موقع التوصيل

### إدارة الخدمات (جدول `services` — كتالوج ديناميكي)
كل خدمة بجدول `services` لها: `name` (فريد)، `icon` (اسم أيقونة من `icons.svg`)، `description`، `badge_type` (`instant`=فوري / `scheduled`=مجدول / `coming`=قريباً — يحدد شارة اللون بصفحة الخدمات)، `action_type` (يحدد إيش يصير عند الضغط على الخدمة)، `min_price` (اختياري)، `time_estimate` (نص حر اختياري)، `sort_order`، `is_active`. زُرعت أول مرة بالبيانات الأربع الأصلية اللي كانت مكتوبة بالكود (وايت ماء، سطحة، حاوية، معدات ثقيلة) بالضبط.

**`action_type` — ثلاث قيم فقط، كل وحدة لها سلوك مختلف كلياً بـ `orderService()`:**
| القيمة | السلوك |
|---|---|
| `order` | يوجّه لـ `order.html?service=NAME` — العميل يحدد السعر بنفسه (يُستخدم `min_price` كحد أدنى) |
| `container` | يوجّه لـ `container-location.html` (تدفق الحاوية الخاص بثلاث خطوات) — **مبني حول خدمة واحدة اسمها "حاوية" فقط**، إضافة خدمة ثانية بنفس `action_type=container` بترجع لنفس تدفق الحاوية العام (مو تدفق منفصل خاص فيها)، لأن `container-location.html`/`container-providers.html` ما يفرّقون بين الخدمات أصلاً — قيد معماري موجود مسبقاً
| `panel` | يفتح لوحة "قريباً" المحلية (`showHeavyEquipment()`) بدون أي طلب فعلي — نفس سلوك "معدات ثقيلة" الحالي |

**التغذية للصفحة الرئيسية:** `app.js`'s `loadServices()` يجلب `GET /api/services/active` ويبني صفوف الخدمات ديناميكياً بدل الأربع صفوف الثابتة الأصلية بالـ HTML — لو فشل الطلب (تعذّر الاتصال بالسيرفر)، الصفوف الأربعة الثابتة الأصلية تبقى ظاهرة كما هي (نفس أسلوب الاحتياط المستخدم بـ `container-location.js` لقائمة المدن). نفس الآلية بـ `order.js`'s `loadServicesData()` (يُستدعى داخل `loadService()` قبل أي منطق آخر) يبني كائن `servicesData` (اسم الخدمة ← icon/type/minPrice/time) بدل الكائن الثابت الأصلي — باقي منطق `order.js` (حساب السعر، التحقق من الحد الأدنى، إلخ) ما تغيّر إطلاقاً، بس صار مصدر البيانات ديناميكي.

### تدفق طلب الحاوية (3 خطوات) — مبني على جدول `products`
خدمة الحاوية فقط (دون باقي الخدمات) تمر بخطوتين قبل صفحة الطلب. العميل يختار **منتج محدد** (لا مزود عام) — كل منتج له حجم وسعر ثابتين له مسبقاً، فما فيه اختيار حجم لاحق بصفحة الطلب:
1. **الموقع** (`container-location.html`): اختيار مدينة (الرياض/جدة/الدمام/مكة/المدينة المنورة) + كتابة الحي → يُحفظ في `localStorage` كـ `yashjub_container_location`
2. **المنتج** (`container-providers.html`): يجلب منتجات الحاوية المتاحة بنفس المدينة من `GET /api/products/available?city=...` (JOIN بين `products` و`providers`)، ويعرض بطاقة لكل **منتج** (اسم، وصف، حجم، سعر الحزمة + عدد أيامها، + اسم وتقييم المزود صاحبه) — عند الاختيار يُحفظ في `localStorage` كـ `yashjub_selected_product`
3. **الطلب** (`order.html?service=حاوية`): يقرأ المنتج المختار ويعرض معلوماته مباشرة (بدون خطوة اختيار حجم)، الحد الأدنى لعدد الأيام يُضبط تلقائياً من `selectedProduct.minDays`. يكمل الطلب بالأيام والتاريخ — يُرسل `providerId`/`providerName`/`productId` مع الطلب. لو حاول المستخدم فتح `order.html?service=حاوية` مباشرة بدون اختيار منتج، يُعاد توجيهه لخطوة الموقع.

**حساب السعر:** كل منتج سعره "حزمة" مقابل `min_days` (مثلاً 600 ريال / 10 أيام)، مو سعر يومي مباشر. السعر اليومي الفعلي = `price ÷ min_days`، والإجمالي لأي عدد أيام (لازم يكون ≥ `min_days`) = ذاك السعر اليومي × عدد الأيام المطلوب (تقسيم خطي، مُقرَّب لأقرب ريال) — نفس المنطق مكرر في `updateContainerPrice()` و`confirmOrder()` بـ `order.js`.

مزود خدمة الحاوية **لا** يحدد مدينة/أسعار عند التسجيل — التسجيل بسيط لكل الخدمات. يضيف منتجاته (كل واحد بمدينته وحيّه وحجمه وسعر حزمته وحد أدنى أيامه الخاص) لاحقاً من قسم "منتجاتي" بلوحة تحكم المزود.

### قسم "منتجاتي" بلوحة المزود
مزود الحاوية (فقط) عنده قسم "📦 منتجاتي" بـ `provider.html` — إدارة كتالوج منتجات (اسم، وصف، حجم، سعر الحزمة، الحد الأدنى للأيام، مدينة، حي، حالة متاح/غير متاح) بعمليات إضافة/تعديل/حذف كاملة، مخزّنة بجدول `products`. **هذا هو المصدر الوحيد** اللي يعتمد عليه تدفق العميل بالكامل (خطوة 2 أعلاه) — أي مزود حاوية بدون منتجات مُضافة **لن يظهر للعملاء أبداً**.

**ملاحظة تاريخية:** أعمدة `providers.city/price_small/medium/large` كانت تُستخدم بنسخة سابقة (سعر ثابت واحد للمزود عند التسجيل) — صارت **غير مستخدمة (deprecated)** بعد التحول لنظام المنتجات، وباقية بالـ schema فقط لعدم كسر بيانات قديمة محلياً. أي مزود حاوية مسجَّل من قبل هالتحديث لازم يضيف منتجاته يدوياً من "منتجاتي" عشان يرجع يظهر.

### تأكيد الطلب (كل الخدمات)
بعد الضغط على "تأكيد الطلب والدفع" بـ `order.html`، ما يترسل الطلب فوراً — تفتح نافذة تأكيد (overlay) فيها ملخص الطلب وشريط عد تنازلي 7 ثواني وزر "إلغاء" يظهر خلال العد بس. لو ما ضغط إلغاء، الطلب يترسل تلقائياً للسيرفر بعد انتهاء الثواني السبع. **بعد التأكيد النهائي، الطلب ما يُلغى** — أزرار "إلغاء الطلب" انحذفت كلياً من `tracking.html` و`my-orders.html` (كانت موجودة قبل). الـ endpoint `PUT /api/orders/:id/status` لسه يقبل `status: 'cancelled'` تقنياً (تستخدمه لوحة الإدارة `admin.js` لإدارة الحالات)، بس ماكو أي واجهة عميل تستدعيه بعد الحين.

## نظام المالية

- رسوم الخدمة: 5% من قيمة كل طلب، تُخصم تلقائياً
- نظام الدفع: Escrow (أمانة) — يُحفظ حتى اكتمال الخدمة
- لا يوجد اشتراك شهري في المرحلة الأولى

## أنواع المستخدمين

**العميل:** تسجيل دخول برقم الجوال + OTP، يطلب الخدمات، يتتبع الطلب، يقيّم المزوّد بعد الاكتمال.

### اسم المستخدم (عميل ومزوّد)
`users.name` — أول مرة يتحقق فيها أي مستخدم (عميل أو مزوّد) من OTP بـ `login.html` وما عنده اسم محفوظ بالسيرفر، تظهر نافذة ترحيب إلزامية (بدون زر إغلاق) تطلب اسمه قبل ما يكمل — تُحفظ عبر `PUT /api/users/:phone/name` وبـ `localStorage` كـ `yashjub_name`. الاسم يظهر بدل "عميل يشجب"/"مزود خدمة" بصفحة `profile.html` (مع زر "تعديل الاسم")، وبسايد بار `index.html` (`app.js`) — **لوحة المزود (`provider.js`) ما تعدّلت لنفس الشي**، لسه تعرض "مزود خدمة" ثابت بسايد بارها الخاص، لأن الطلب الأصلي حصر التعديل بـ `app.js`. `yashjub_name` يُمسح من localStorage مع باقي بيانات الجلسة عند تسجيل الخروج (بكل نقاط الخروج الثلاث: `app.js`, `profile.js`, `provider.js`).

### مطابقة المزود بالطلب + التواصل (`tracking.html`)
كل طلب (بما فيها وايت ماء/سطحة، مو بس الحاوية) يرتبط بمزود حقيقي وقت `POST /api/orders`: لو الطلب ما حدد `providerId` صراحة (حالة الحاوية اللي يحدده العميل بنفسه)، السيرفر يطابق تلقائياً أفضل مزود متاح (`is_available=1`) بنفس نوع الخدمة (الأعلى تقييماً) من جدول `providers`. لو ماكو أي مزود مسجّل لهذي الخدمة، الطلب يكمل بدون مزود (`provider_id=null`) بدون أخطاء.

صفحة `tracking.html` تعرض قسم "التواصل مع المزود" (اسم + تقييم) فقط لو فيه مزود مرتبط فعلياً — زر اتصال مباشر (`tel:+966...`) وزر محادثة يفتح شات حقيقي محفوظ بالسيرفر (راجع قسم "نظام المحادثات" بالأسفل)، يختفي القسم تلقائياً عند اكتمال محاكاة الخدمة (المحادثة نفسها تبقى محفوظة بقاعدة البيانات للمرجعية، ما تُحذف).

**المزوّد (مستويان — المستوى الأساسي حُذف):**
| المستوى | المتطلبات | المميزات |
|---|---|---|
| موثّق (2) | هوية + جوال + IBAN + شهادة العمل الحر | ظهور أعلى + شارة توثيق |
| شركة (3) | + سجل تجاري | ظهور كامل + أولوية |

كل تسجيل يتطلب رفع فعلي (ملف صورة أو PDF، ≤5MB) لصورة الهوية/الإقامة (إلزامي دائماً)، وملف شهادة العمل الحر (مستوى 2) أو السجل التجاري (مستوى 3). الملفات تُخزَّن محلياً بمجلد `uploads/` (عبر `multer`، مستثنى من git بـ `.gitignore`) وتُقدَّم كملفات ثابتة على `/uploads/...`. **تنبيه:** استضافة Render الحالية (الخطة المجانية) بدون تخزين دائم — أي ملف مرفوع (وكل قاعدة البيانات SQLite نفسها) ينمحي مع أي إعادة نشر/تشغيل جديد للموقع الحي. قرار مقبول مؤقتاً؛ لو احتجنا استمرارية حقيقية لازم ننتقل لتخزين سحابي (مثل Cloudinary) وقاعدة بيانات مُدارة بدل SQLite المحلي.

**المدير (Admin):** `admin` / `يشجب` مع كلمة السر `yashjub2025` — رابط `/admin.html` — صفحة الموردين فيها روابط تفتح مستندات كل مزود (الهوية/الشهادة) بتبويب جديد للمراجعة.

### إدارة الموظفين وصلاحيات لوحة الإدارة
نفس صفحة دخول لوحة الإدارة (`/admin.html`) تقبل نوعين من بيانات الدخول: المدير العام (بيانات ثابتة بالأعلى، `admin.js`) أو موظف مُسجَّل بجدول `employees` (اسم مستخدم + كلمة مرور مشفَّرة bcrypt، عبر `POST /api/employees/login`). `doLogin()` يجرّب المدير العام أولاً، ولو ما طابق يجرّب تسجيل دخول الموظف.

قسم "📋 إدارة الموظفين" بالسايد بار (أيقونة `icon-id-card`) **يظهر فقط للمدير العام** أو لموظف بمنصب `admin` — إضافة/تعديل/حذف موظفين، كل موظف له اسم كامل + جوال + إيبان + منصب + اسم مستخدم + كلمة مرور + حالة نشط/موقوف. تعديل موظف بدون تعبئة حقل كلمة المرور يبقيها كما هي (السيرفر ما يحدّثها لو الحقل فاضي).

**صلاحيات كل منصب** (`ROLE_PAGES` بـ `admin.js`) — تتحكم بأي أقسام سايد بار تظهر للموظف بعد دخوله، والصفحة الافتراضية بعد الدخول هي أول قسم مسموح له:
| المنصب | الصفحات المتاحة |
|---|---|
| `admin` | كل الصفحات (بدون قيود) + إدارة الموظفين |
| `supervisor` | الطلبات، الموردون، العملاء |
| `support` | الطلبات، البلاغات والشكاوى |
| `reviewer` | طلبات التوثيق فقط |
| `accountant` | المدفوعات، العمولات |

**تنبيه أمني معروف ومقبول:** الصلاحيات هذي كلها من جهة العميل (client-side) فقط — نفس أسلوب حماية لوحة الإدارة الحالي أصلاً (كلمة سر المدير العام نفسها مكتوبة صراحة بـ `admin.js` القابل للقراءة من أي متصفح، وكل endpoints الـ API بدون أي تحقق من هوية المتصل). إخفاء أقسام السايد بار يمنع الاستخدام العرضي فقط، وما يمنع موظف تقني من استدعاء أي API مباشرة. لو احتجنا حماية حقيقية لاحقاً، لازم نضيف نظام جلسات/tokens فعلي بجانب middleware تحقق صلاحيات بكل endpoint حساس.

### الخرائط (Leaflet + OpenStreetMap — مجاني بالكامل، بدون مفتاح API)
تحديد المواقع بالمشروع يعتمد على خرائط Leaflet مع بلاطات OpenStreetMap، تُحمَّل عبر CDN (`unpkg.com/leaflet@1.9.4`) بكل صفحة معنية — بدون حزمة npm. مركز الخريطة الافتراضي: الرياض `[24.7136, 46.6753]`.

- **`order.html`**: خريطة تفاعلية بدل حقل العنوان النصي القديم — الضغط عليها (أو زر "استخدم موقعي الحالي" عبر `navigator.geolocation`) يحط pin ويعبّي حقل العنوان تلقائياً عبر **Geocoding عكسي بـ Nominatim** (`nominatim.openstreetmap.org/reverse`)، ويخزّن الإحداثيات بحقلين مخفيين (`lat`/`lng`) تُرسل مع الطلب. **حقل عنوان التوصيل الثاني الخاص بالسطحة يبقى نص عادي** (لم يتحول لخريطة).
- **`tracking.html`**: خريطة ثابتة (بدون تفاعل) بـ pin على موقع الطلب — تظهر فقط لو الطلب فيه إحداثيات محفوظة (طلبات قبل هالتحديث تُستثنى بهدوء).
- **`provider.html`** (فورم "منتجاتي"): نفس مكوّن اختيار الموقع (خريطة + GPS) — هذا هو **المصدر الوحيد** لإحداثيات المنتجات؛ بدونه ما تظهر أي pins بصفحة اختيار المنتج للعميل.
- **`container-providers.html`**: خريطة تعرض pin لكل منتج عنده إحداثيات (مع فقاعة اسم/سعر/مزود عند الضغط) — المنتجات بدون إحداثيات تبقى بالقائمة النصية تحتها بس بدون pin.

**قيد مهم:** Nominatim مجاني لكن بسياسة استخدام عادلة (~طلب واحد/ثانية، بدون استخدام مكثف) — مناسب لحجم المشروع الحالي. لو فشل الطلب أو تأخر، الكود يرجع للإحداثيات الخام كنص بدل ما يكسر التدفق.

الكود مكرر يدوياً بكل ملف JS معني (`order.js`, `tracking.js`, `provider.js`, `container-providers.js`) — المشروع ما فيه ملف JS مشترك أصلاً، فهذا يحافظ على نفس الأسلوب البسيط الحالي.

### الأيقونات (SVG sprite — `icons.svg`)
كل أيقونات الموقع تحوّلت من إيموجي يونيكود ملوّن (🚚📍⭐...) لأيقونات SVG بسيطة، لأن الإيموجي الملوّن **لا يتأثر بـ CSS `color`** بأي متصفح فمستحيل يكون أسود بالوضع العادي وأبيض بالوضع الداكن وهو باقي إيموجي.

`icons.svg` بجذر المشروع — **أول ملف مشترك فعلي بالمشروع غير `style.css`** — يحتوي ~68 تعريف `<symbol id="icon-NAME" viewBox="0 0 24 24">` (SVG مخفي، مجرد مستودع تعريفات). أي أيقونة بأي صفحة تُستخدم كـ:
```html
<svg class="icon"><use href="icons.svg#icon-truck"></use></svg>
```
مدعوم بكل المتصفحات الحديثة بدون مكتبة أو build step. الأيقونة تاخذ لونها من `fill: currentColor` (قاعدة `.icon` بـ `style.css`) فتتبع تلقائياً لون النص المحيط بها — أسود بالوضع العادي وأبيض بالوضع الداكن بدون أي كود خاص بالثيم، بنفس آلية ألوان النصوص الموجودة أصلاً عبر `body.dark-mode`.

بملفات JS اللي فيها جداول أيقونات ديناميكية (`servicesData` بـ`order.js`, `serviceIcons` بـ`my-orders.js`/`provider.js`, `levelBadges` بـ`container-providers.js`, `levelLabel` بـ`admin.js`...) القيمة صارت اسم الأيقونة (`icon: "truck"`) بدل حرف الإيموجي، ونقطة الاستخدام تبني وسم `<svg>` كامل عبر `.innerHTML` بدل `.textContent`.

**مستبعد من هذا التحويل عمداً (يبقى إيموجي نصي كما هو):** كل إيموجي داخل `alert()` (نوافذ المتصفح تعرض نص فقط، ما تستضيف SVG)، وكل إيموجي داخل `console.log` بـ `server.js`/`database.js` (رسائل طرفية وقت التشغيل، ما يشوفها أي مستخدم)، ورمز © وأسهم/رموز الواجهة المحايدة (→ ← ☰ ✕) اللي أصلاً تتبع لون النص الحالي.

## هيكل الملفات

```
yashjub/
├── index.html / app.js              الصفحة الرئيسية
├── login.html / login.js            تسجيل الدخول
├── order.html / order.js            صفحة الطلب (خطوة 3 للحاوية)
├── container-location.html / .js    اختيار مدينة/حي الحاوية (خطوة 1)
├── container-providers.html / .js   اختيار مزود الحاوية (خطوة 2)
├── tracking.html / tracking.js      تتبع الطلب
├── my-orders.html / my-orders.js    طلباتي
├── provider.html / provider.js      لوحة المزوّد
├── register-provider.html / .js     تسجيل مزوّد جديد
├── profile.html / profile.js        الملف الشخصي
├── admin.html / admin.js            لوحة الإدارة
├── about.html, contact.html, terms.html, 404.html
├── server.js                        السيرفر الرئيسي (Express)
├── database.js                      إعداد قاعدة البيانات
├── style.css                        التصميم الكامل
├── icons.svg                        مكتبة أيقونات SVG المشتركة (كل الموقع)
├── yashjub.db                       قاعدة بيانات SQLite (مستثناة من git)
└── images/                          logo.png, banner.jpeg
```

## التقنيات

**Frontend:** HTML5, CSS3, JavaScript (Vanilla) — خط Cairo من Google Fonts — RTL دائماً (عربي أولاً) — Responsive (جوال أولاً) — Leaflet.js + OpenStreetMap للخرائط (CDN، بدون مفتاح API)

**قاعدة البيانات (SQLite):**
```sql
users       -- phone, otp, verified, name, suspended_until
orders      -- phone, service, address, price, commission, status, provider_id, provider_name, provider_phone, product_id, lat, lng, coupon_code, discount, accepted_at
providers   -- phone, name, service_type, level, rating, id_document_path, certificate_path, suspended_until, city*, price_small*, price_medium*, price_large*  (*deprecated، غير مستخدمة)
products    -- provider_id, name, description, size, price, min_days, city, neighborhood, is_available, lat, lng
employees   -- name, phone, iban, role (admin/supervisor/support/reviewer/accountant), username, password (مشفّرة bcrypt), is_active
cities      -- name, is_active — مزروعة بـ 5 مدن أساسية أول تشغيل (لو الجدول فاضي)
coupons     -- code (فريد، UPPERCASE دايماً)، discount_type (percent/fixed)، discount_value، max_discount، usage_limit، used_count، expires_at، is_active
services    -- name (فريد)، icon، description، badge_type (instant/scheduled/coming)، action_type (order/container/panel)، min_price، time_estimate، sort_order، is_active — مزروعة بالخدمات الأربع الأصلية أول تشغيل
chats       -- order_id، sender (client/provider/admin)، sender_phone، message، is_read (يمثّل "قرأتها الإدارة" فقط)
notifications -- title، message، type (alert/offer/update/urgent)، target (all/clients/providers/specific)، target_phone، receiver_phone، is_read — صف واحد لكل مستلم حتى بالبث الجماعي
complaints  -- order_id، reporter_phone، reporter_type (client/provider)، reported_phone، reported_type (client/provider)، type، description، status (new/reviewing/resolved/closed)، admin_note، action_taken (سجل تراكمي — كل إجراء يُضاف سطر جديد له لا يستبدل القديم)، resolved_at
reviews     -- order_id، reviewer_phone، reviewer_type (client/provider)، reviewed_phone، reviewed_type (client/provider)، rating (1-5)، comment، admin_reply، is_visible، is_flagged
```
`price` بجدول `products` هو سعر حزمة كاملة (مو سعر يومي) — دايماً يُقرأ مع `min_days` المرافق له. `provider_id`/`provider_name`/`product_id` بالطلبات اختيارية (تُملأ فقط لطلبات الحاوية). `orders.price` يخزَّن **بعد** خصم الكوبون (لو فيه) — و`orders.discount`/`orders.coupon_code` يحفظان قيمة ومصدر الخصم للمرجعية. أعمدة `providers.city/price_*`، `orders.provider_id/provider_name/product_id/coupon_code/discount`، و`products.min_days` أُضيفت عبر `ALTER TABLE` مغلّفة بـ try/catch في `database.js` (migration بسيط بدون ORM).

### كوبونات الخصم
العميل يكتب الكود بحقل "كود الخصم" بصفحة `order.html` (لكل الخدمات) ويضغط "تطبيق" → `POST /api/coupons/validate` يتحقق من الكود (نشط، ما انتهت صلاحيته، ما تجاوز حد الاستخدام) ويرجّع قيمة الخصم كمعاينة فقط (بدون احتساب استخدام). العميل يشوف صف "الخصم" بملخص السعر يتحدّث تلقائياً كل ما يغيّر السعر/الأيام (نفس منطق حساب الخصم مكرر بـ `order.js`'s `computeDiscount()` مطابق تماماً لدالة `checkCoupon()` بـ `server.js` عشان المعاينة تطابق القيمة الفعلية). عند تأكيد الطلب، `couponCode` يُرسل مع الطلب، والسيرفر هو المصدر الوحيد الموثوق لحساب الخصم والتحقق النهائي (يمنع أي تلاعب من جهة المتصفح) — يطرح الخصم من `price` قبل حساب العمولة 5%، ويزيد `used_count` بجدول `coupons` بمقدار 1.

### المدن
جدول `cities` هو المصدر الوحيد لقائمة المدن بخطوة اختيار موقع الحاوية (`container-location.html`) — يُحمَّل ديناميكياً عبر `GET /api/cities/active` (`container-location.js`)، مع بقاء القائمة الثابتة بالـ HTML كنسخة احتياطية لو فشل الطلب. لوحة الإدارة (قسم "المدن") تدير الجدول بالكامل (إضافة/تعديل/حذف/تفعيل). **ملاحظة:** باقي أماكن المشروع (منتجات الحاوية، تسجيل مزوّد) لسه ما تربط بهذا الجدول — فقط خطوة اختيار الموقع للعميل.

### مركز العمليات المباشر (لوحة الإدارة)
قسم "مركز العمليات" بلوحة الإدارة (`admin.js`'s `startOperationsPage()`) يعرض حالة المنصة اللحظية ويحدّث نفسه تلقائياً كل 30 ثانية (`setInterval` يبدأ عند دخول الصفحة ويتوقف تلقائياً عند مغادرتها عبر `showPage()`). يجمع بيانات من 3 مصادر: `GET /api/operations/live` (إحصائيات + تنبيهات محسوبة بالسيرفر)، و`GET /api/orders`/`GET /api/providers` (للجدول والخريطة والقوائم، بنفس أسلوب باقي صفحات لوحة الإدارة).

- **متوسط وقت القبول**: يعتمد على عمود `orders.accepted_at` (يُسجَّل تلقائياً أول مرة تتغيّر حالة الطلب إلى `accepted` عبر `PUT /api/orders/:id/status` — تحديثات لاحقة ما تغيّره). الطلبات القديمة (قبل هالتحديث) ما فيها هذي القيمة، فالمتوسط يُحسب من آخر 50 طلب فيه `accepted_at` فقط.
- **الخريطة**: تعرض pin أحمر لكل طلب نشط له إحداثيات (`orders.lat/lng`، نفس حقول خريطة `order.html`)، وpin أخضر لكل مزود متاح له إحداثيات. **مزودو وايت ماء/سطحة ما عندهم أي إحداثيات مسجّلة بالنظام أصلاً** (ماكو تتبع GPS حي للمزوّد) — الـ pin الأخضر يظهر فقط لمزودي الحاوية اللي عندهم منتج واحد على الأقل بإحداثيات (`GET /api/providers` يجيب `lat`/`lng` بـ subquery من آخر منتج للمزوّد له إحداثيات). مزوّد بدون أي منتج بإحداثيات ببساطة ما يظهر بالخريطة (يبقى بقائمة "المزودين المتاحين الآن" النصية تحتها).
- **"آخر نشاط" بقائمة المزودين**: ماكو نظام تتبع حضور/heartbeat حقيقي بالمشروع لأي مزوّد — القيمة المعروضة هي وقت آخر طلب فعلي ارتبط بهذا المزوّد (`provider_id`)، أو "لا يوجد نشاط بعد" لو ما له أي طلب.
- **تنبيه "شكوى جديدة"** بمركز العمليات نفسه **لسه غير موجود** (رغم إن نظام الشكاوى صار حقيقي — راجع قسم "نظام البلاغات والشكاوى" بالأسفل) — تنبيهات الشكاوى (شكوى جديدة/بدون حل 24 ساعة/تكرار ضد شخص واحد) تظهر بنظام الإشعارات وصفحة "البلاغات والشكاوى" مباشرة، مو بقائمة تنبيهات مركز العمليات (اللي تبقى مختصة بالطلبات والمزودين فقط). التنبيهات الفعلية المُنفَّذة هنا: طلب بدون مزود منذ ≥10 دقائق (أحمر) أو ≥5 دقائق (أصفر) — تُستبعد الطلبات الأقدم من 24 ساعة من التنبيهات (تبقى بالجدول فقط) لتفادي إغراق القائمة بطلبات قديمة مهجورة — ومزوّد جديد انضم خلال آخر 24 ساعة يحتاج مراجعة (أزرق، بديل معقول عن "طلب توثيق معلّق" الحقيقي لأن التسجيل الحالي auto-approve بدون طابور توثيق فعلي).
- **"تدخل يدوي"**: يفتح نافذة لاختيار مزوّد متاح بنفس نوع الخدمة وتعيينه للطلب عبر `PUT /api/orders/:id/assign-provider` (endpoint جديد، يحدّث `provider_id`/`provider_name`/`provider_phone` بالطلب مباشرة — يشتغل مع أي طلب سواء كان بدون مزوّد أصلاً أو لتغيير مزوّده الحالي).
- **"تواصل" مع مزوّد**: رابط `tel:` مباشر لجواله (نفس أسلوب التواصل بـ `tracking.html`)، بدون نظام مراسلة جديد.

### نظام المحادثات (جدول `chats` — عميل/مزوّد/إدارة)
كل رسالة محفوظة بجدول `chats` مربوطة بـ `order_id`، ولها `sender` (`client`/`provider`/`admin`). ثلاث واجهات تستخدم نفس الجدول:

- **العميل** (`tracking.html`): قسم "التواصل مع المزود" يظهر فقط لو فيه مزود مرتبط، يرسل ويستقبل عبر `sender: 'client'`، ويحدّث نفسه تلقائياً كل 10 ثواني (`setInterval` يبدأ عند ظهور القسم ويتوقف عند اكتمال الخدمة).
- **المزوّد** (`provider.html`): كل بطاقة طلب "مقبول" فيها زر "محادثة العميل" يفتح لوحة شات مضمّنة بنفس البطاقة (`sender: 'provider'`) — محادثة وحدة بس تكون مفتوحة بنفس الوقت (فتح ثانية يسكّر الأولى تلقائياً)، تحديث كل 10 ثواني بس أثناء ما اللوحة مفتوحة.
- **الإدارة** (`admin.html` قسم "المحادثات"): يعرض كل المحادثات مجمّعة حسب الطلب (`GET /api/chats`) مع فلاتر (الكل/نشطة/مكتملة/تحتاج تدخل) وبحث برقم الطلب أو اسم/جوال العميل. الضغط على "عرض المحادثة" يفتح panel جانبي (`GET /api/chats/:orderId`) يعرض كل الرسائل بفقاعات متمايزة الألوان حسب المرسل، ويرسل كـ `sender: 'admin'`. فتح الـ panel يستدعي `PUT /api/chats/:orderId/read` تلقائياً (يصفّر عداد غير المقروء لهذي المحادثة بالذات).

**"تحتاج تدخل" محسوبة بالسيرفر** (`GET /api/chats`، حقل `needs_intervention`): الطلب لسه نشط (`pending`/`accepted`) **و** آخر رسالة بالمحادثة عمرها أكثر من 30 دقيقة بدون أي رد جديد — بديهياً طلبات `completed`/`cancelled` ما تحتاج تدخل أبداً بغض النظر عن قدم آخر رسالة. عدد المحادثات المحتاجة تدخل يظهر كـ badge أحمر بجانب "المحادثات" بالسايد بار (`chatsBadge`)، ويتحدّث من `loadDashboard()` كمان عشان يبان من أي صفحة بلوحة الإدارة مو بس صفحة المحادثات نفسها.

**ملاحظة تقنية:** عمود `is_read` يمثّل "قرأتها الإدارة" فقط (مو حالة قراءة منفصلة لكل طرف) — لأن الشات الفعلي بين العميل والمزوّد لحظي عبر الاستطلاع الدوري (polling)، وحالة "مقروء/غير مقروء" مفهوم يهم لوحة الإدارة تحديداً (لمعرفة أي محادثات تحتاج مراجعة).

### نظام الإشعارات (جدول `notifications`)
كل صف بجدول `notifications` مرسل لمستلم واحد (`receiver_phone`) — إرسال جماعي (كل العملاء/كل المزودين/الجميع) يُنشئ صف مستقل لكل مستلم بنفس العنوان/الرسالة/النوع/وقت الإرسال (ماكو عمود "batch id" منفصل). لوحة الإدارة (`admin.js`'s `groupNotifications()`) تجمّع هذي الصفوف بواجهة العميل حسب (العنوان+الرسالة+النوع+وقت الإرسال) عشان تعرض "حدث إرسال واحد" بعدد مستلمين إجمالي بدل صف مكرر لكل شخص — التجميع يشتغل بثقة لأن كل صفوف نفس الإرسال تُنشأ بنفس الطلب (نفس الثانية بالضبط، `datetime('now')` بدقة ثانية وحدة).

**الإرسال اليدوي** (قسم "الإشعارات" بلوحة الإدارة): 4 خيارات مستلم (كل العملاء/كل المزودين/الجميع بدون تكرار/مستخدم محدد)، مع معاينة حية لعدد المستلمين المتوقع قبل الإرسال (محسوبة من `GET /api/users`+`GET /api/providers` المحمّلة أصلاً بلوحة التحكم الرئيسية). أزرار "حذف" و"تعيين الكل كمقروء" بسجل الإشعارات تشتغل عبر استدعاء `DELETE`/`PUT .../read` **لكل صف بالمجموعة على حدة** (loop بجهة العميل) لأن الـ API المتاح أصلاً بمستوى الصف الواحد فقط، ماكو endpoint جماعي.

**فلاتر سجل الإشعارات "للعملاء"/"للمزودين"** تتحقق من `receiver_phone` مقابل قائمتي أرقام العملاء/المزودين المخزّنتين مؤقتاً بالمتصفح (`cachedClientPhones`/`cachedProviderPhones`) — مو من عمود `target` مباشرة، لأن إشعارات النظام التلقائية لمستلم واحد (زي "طلب جديد بانتظارك" لمزوّد معيّن) قيمة `target` فيها `specific` مو `providers`، فالفلترة الصحيحة تحتاج تتحقق من هوية المستلم الفعلي.

**الإشعارات التلقائية المُنفَّذة فعلياً** (كلها تستدعي `createNotification()` بـ`server.js`):
| الحدث | المستلم | نوع الإشعار |
|---|---|---|
| طلب جديد له مزوّد (صراحة أو مطابقة تلقائية) | المزوّد المُسنَد للطلب تحديداً | `update` |
| تغيّر حالة الطلب إلى `accepted` | العميل | `update` |
| تغيّر حالة الطلب إلى `completed` | العميل | `update` |
| تسجيل مزوّد جديد | الإدارة (`receiver_phone='admin'`، رقم وهمي ثابت — ماكو مفهوم "رقم جوال الإدارة" بالمشروع أصلاً) | `update` |
| طلب بدون مزوّد منذ أكثر من 10 دقائق | الإدارة | `urgent` |
| شكوى جديدة | الإدارة | `urgent` |
| شكوى بدون حل منذ أكثر من 24 ساعة | الإدارة | `urgent` |
| 3 شكاوى ضد نفس الشخص (تُطلق مرة وحدة بالضبط عند الوصول لـ 3) | الإدارة | `urgent` |

**ملاحظة مهمة على تفسير "طلب جديد → إشعار للمزودين المتاحين":** الطلب الأصلي بالتصميم افترض بث الإشعار لكل المزودين المتاحين (نظام "مزادة مفتوحة"). بس بنية المشروع الفعلية تُسنِد كل طلب لمزوّد واحد فقط تلقائياً وقت الإنشاء (`POST /api/orders`) — ماكو نظام "أي مزوّد يقدر ياخذ الطلب" أصلاً. فتنفيذه الصحيح المطابق للواقع: إشعار للمزوّد الوحيد المُسنَد فعلياً، مو بث لكل المزودين (بث حقيقي كان بضلل بقية المزودين برسائل عن طلبات ما راح توصلهم أبداً).

**تنبيهات "طلب بدون مزوّد" و"شكوى بدون حل" ماهي مبنية على polling من المتصفح** — `server.js` فيه `setInterval` حقيقي يفحص كل 5 دقائق (`checkStaleOrders()`/`checkStaleComplaints()`) بدون أي اعتماد على وجود أدمن مسجّل دخول أو فاتح صفحة معيّنة، مع `Set` بالذاكرة لكل واحد (`notifiedStaleOrders`/`notifiedStaleComplaints`) يمنع تكرار نفس الإشعار أكثر من مرة (تُصفَّر لو أعيد تشغيل السيرفر — نفس فلسفة عدم-الاستمرارية المقبولة أصلاً بباقي الحالات المؤقتة بالمشروع).

**واجهة العميل** — جرس إشعارات بهيدر `index.html` (`app.js`) يظهر فقط لو المستخدم مسجّل دخول، مع badge أحمر لعدد غير المقروء، وdropdown يعرض آخر 5 إشعارات (يتحدّث تلقائياً كل 60 ثانية)، وزر "عرض الكل" يوديك لقسم "إشعاراتي" بـ`profile.html#notifications` (قائمة كاملة، الضغط على أي إشعار يعلّمه كمقروء). صفحة `profile.html` ما فيها جرس/dropdown منفصل خاص فيها — بس عداد "X غير مقروءة" نصي بعنوان القسم، لتفادي تكرار نفس آلية الجرس بصفحتين مختلفتين لغرض واحد.

### نظام البلاغات والشكاوى (جدول `complaints`)
العميل يقدّم شكوى من `my-orders.html` (زر "تقديم شكوى" يظهر فقط على طلب `completed`/`cancelled`)، والمزوّد يقدّم بلاغ بنفس الطريقة من `provider.html` (زر "تقديم بلاغ" بنفس الشرط) — نفس الفورم بالضبط (نوع + تفاصيل حرة) مكرر بالصفحتين، يرسل لنفس `POST /api/complaints` بس بـ`reporterType` مختلف (`client` أو `provider`) و`reportedType` المقابل تلقائياً (لو أبلغ عميل، `reportedType` تصير `provider` والعكس). بعد الإرسال يظهر رقم الشكوى للمتابعة برسالة تأكيد.

**النوع (`type`) قائمة ثابتة مكررة بكل مكان يظهر فيها:** تأخر، سوء تعامل، رفض دفع، لم يحضر، جودة رديئة، عنوان خاطئ، إلغاء متكرر، أخرى.

**صفحة "البلاغات والشكاوى" بلوحة الإدارة** — إحصائيات لحظية (جديدة/قيد المراجعة/محلولة هذا الشهر/متوسط وقت الحل — محسوبة كلها client-side من `GET /api/complaints` بدون endpoint تجميعي منفصل)، فلاتر بالحالة، بحث برقم الشكوى/الطلب/الجوال، وpanel جانبي للتفاصيل يجمع 3 مصادر عند الفتح: `GET /api/complaints/:id` (بيانات الشكوى + JOIN مع `orders`/`users`/`providers` لأسماء المُبلِّغ والمُبلَّغ عنه)، و`GET /api/orders/:id` + `GET /api/chats/:orderId` (لو الشكوى مرتبطة بطلب) لعرض تفاصيل الطلب وسجل محادثته الكامل — إعادة استخدام مباشر لـ endpoints موجودة أصلاً من ميزتي الطلبات والمحادثات، بدون أي endpoint جديد مخصص لهذا.

**الإجراءات السبعة** كلها تمر عبر `PUT /api/complaints/:id/action` بـ`actionType` مختلف (نفس endpoint واحد لكل الأزرار، مو 7 endpoints منفصلة):
| الزر | السلوك |
|---|---|
| ✅ تم الحل | `status → resolved`، يسجّل `resolved_at` |
| 💰 استرداد المبلغ | **تسجيل إداري فقط بـ`action_taken` + إشعار للعميل** — ماكو نظام دفع/Escrow حقيقي بالمشروع أصلاً (نفس القيد الموثّق بقسم "نظام المالية")، فمافيه مبلغ حقيقي يُرد فعلياً ولا تتغيّر بيانات الطلب المالية؛ الإجراء بيان نية موثّق وإشعار للعميل بس |
| ⚠️ إرسال تحذير | إشعار `urgent` مباشر للمُبلَّغ عنه عبر نظام الإشعارات |
| 🚫 إيقاف مؤقت (يوم/أسبوع/شهر) | يحسب `suspended_until` ويحفظه بجدول المُبلَّغ عنه (`providers` أو `users`) — **إيقاف حقيقي بأثر فعلي**: مزوّد موقوف يصير `is_available=0` (يختفي من كل مطابقة طلبات تلقائية ومن الخريطة)، وأي محاولة تسجيل دخول (`POST /api/auth/verify-otp`) لعميل أو مزوّد موقوف تُرفض برسالة صريحة توضح تاريخ الانتهاء. فحص دوري كل 5 دقائق (`reactivateSuspendedAccounts()`) يعيد التفعيل تلقائياً بعد انتهاء المدة |
| 🗑️ حذف الحساب نهائياً | `DELETE` فعلي من جدول `providers` أو `users` — يتطلب تأكيد (`confirm()`) بجهة العميل قبل الإرسال، ولا يمكن التراجع عنه |
| 💬 إرسال رد | إشعار `update` للمُبلِّغ (مو المُبلَّغ عنه) بنص الرد المكتوب |
| ❌ إغلاق بدون إجراء | `status → closed`، يسجّل `resolved_at` بدون أي تأثير على أي حساب |

كل إجراء يُضاف كسطر جديد بعمود `action_taken` (تراكمي، مو استبدال) بصيغة `[توقيت] وصف الإجراء` — يعطي سجل كامل لكل ما اتخذته الإدارة بهذي الشكوى بعمود واحد بدون جدول منفصل. حقل الملاحظة بالـ panel (`compActionNote`) يُستخدم مباشرة كـ"سبب" الإجراء ولنص الرد بزر "إرسال رد" بنفس الوقت (حقل واحد بدور مزدوج، بدل حقلين منفصلين).

**تنبيه "تكرار 3 شكاوى ضد نفس الشخص"** يُطلق بالضبط عند وصول `COUNT(*) WHERE reported_phone = ?` لـ 3 (مو ≥3) عشان ما يتكرر التنبيه مع كل شكوى رابعة وخامسة وهكذا.

### نظام التقييمات (جدول `reviews`)
العميل يقيّم الخدمة من `my-orders.html` (زر "تقييم الخدمة" يظهر فقط على طلب `completed`، ويختفي بعد الإرسال ويتحول لـ"تم التقييم ⭐" — العميل مايقدر يقيّم نفس الطلب مرتين، السيرفر يرفض المحاولة الثانية صراحة). 5 نجوم تفاعلية + تعليق نصي اختياري، يرسل لـ`POST /api/reviews` بـ`reviewerType: 'client'` و`reviewedType: 'provider'`.

**`providers.rating` صار عمود محسوب حقيقي** (مو ثابت افتراضي 5.0 زي قبل) — `recalculateProviderRating()` بـ`server.js` تعيد حساب المتوسط من كل تقييمات المزوّد **الظاهرة فقط** (`is_visible=1`) في 3 لحظات: عند إضافة تقييم جديد، عند تغيير حالة الظهور من لوحة الإدارة، وعند حذف تقييم. هذا يجعل التقييم يظهر بشكل حقيقي بكل مكان يُستخدم فيه أصلاً بالمشروع (مطابقة أفضل مزود تلقائياً للطلبات، ترتيب `container-providers.html`، قوائم لوحة الإدارة) بدون أي تعديل على تلك الأماكن نفسها.

**لوحة المزود** — قسم "تقييماتي" (`provider.js`'s `loadMyReviews()`) يعرض متوسط التقييم الحقيقي (بدل الرقم الثابت 4.8 اللي كان مكتوب صراحة بالـ HTML وما يتغيّر أبداً)، عدد التقييمات، مؤشر بياني بسيط (أعمدة CSS ملوّنة حسب كل تقييم، بدون أي مكتبة رسوم بيانية) لآخر 10 تقييمات بالترتيب الزمني، وآخر 5 تقييمات كاملة مع التعليقات ورد الإدارة إن وُجد. بطاقة الهيدر وبطاقة الإحصائيات بنفس الصفحة صارت تعرض `providers.rating` الحقيقي بدل "4.8" الثابت أيضاً (نفس المصدر، إصلاح مباشر مرتبط بهذي الميزة).

**`container-providers.html`** يعرض عدد التقييمات بجانب النجوم (`provider_review_count`، عمود جديد بـ`GET /api/products/available` عبر subquery على `reviews`) — التقييم نفسه كان معروض من قبل أصلاً (`provider_rating`)، الإضافة هنا بس العدد.

**لوحة الإدارة** — صفحة "التقييمات" فيها إحصائيات لحظية (المتوسط العام، الإجمالي، نسبة الراضين 4-5⭐، نسبة غير الراضين 1-2⭐ — كلها من `GET /api/reviews/stats`)، توزيع النجوم كأشرطة نسبة مئوية، أفضل وأسوأ 3 مزودين (بدون حد أدنى لعدد التقييمات — العدد معروض بجانب كل مزوّد عشان الإدارة تقدّر السياق بنفسها)، فلاتر وبحث، وpanel تفاصيل يعرض تقييم عكسي لنفس الطلب لو موجود (`reverseReview` — مثلاً تقييم المزوّد للعميل بنفس الطلب، رغم إنه ماكو واجهة فعلية حالياً ترسل هذا الاتجاه، بس الـ schema والعرض جاهزين له لو أُضيف لاحقاً).

**الإجراءات الأربعة** كلها عبر endpoints منفصلة (مو endpoint موحّد زي الشكاوى): موافق/إخفاء (`PUT .../visibility`، يُعيد حساب تقييم المزوّد فوراً)، تحديد كمشبوه (`PUT .../flag`، يطلق إشعار للإدارة)، رد الإدارة (`PUT .../reply`، يُرسل إشعار حقيقي للمُقيِّم عبر نظام الإشعارات)، وحذف (`DELETE`، بتأكيد، يُعيد حساب التقييم أيضاً).

**التنبيهات التلقائية:** تقييم سلبي (1-2⭐) → إشعار فوري للإدارة، 3 تقييمات سلبية ضد نفس الشخص (`COUNT(*) WHERE reviewed_phone = ? AND rating <= 2` يساوي 3 بالضبط) → تنبيه عاجل، وتقييم مشبوه → إشعار عند لحظة التحديد الفعلي من لوحة الإدارة (مافيه كشف تلقائي للمحتوى المشبوه — ماكو نظام تحليل نص/spam بالمشروع أصلاً، التحديد قرار بشري دايماً).

**API Endpoints:**
```
POST /api/auth/send-otp
POST /api/auth/verify-otp                     الاستجابة تتضمن name (فاضي لو المستخدم ما حدد اسمه بعد)
PUT  /api/users/:phone/name                    body: { name } — حفظ/تعديل اسم المستخدم (عميل أو مزوّد)
POST /api/orders                              body إضافي اختياري: providerId, providerName, productId, lat, lng, couponCode — لو providerId غايب يُطابَق مزود تلقائياً حسب service، ولو couponCode موجود يُتحقق منه ويُخصم من price قبل حساب العمولة
GET  /api/orders
GET  /api/orders/:id
GET  /api/orders/user/:phone                  يشمل is_reviewed (هل قيّم هذا العميل هذا الطلب فعلاً) — يتحكم بزر "تقييم الخدمة" بـ my-orders.html
PUT  /api/orders/:id/status                   أول تحويل لـ accepted يسجّل accepted_at تلقائياً (للمرة الأولى فقط)
PUT  /api/orders/:id/assign-provider           body: { providerId } — تعيين/تغيير مزوّد الطلب يدوياً (مركز العمليات)
GET  /api/users                               يشمل name لكل مستخدم
POST /api/providers/register                  multipart/form-data: fullName, phone, idNumber, idDocument(ملف), iban, serviceType, level (2/3) + حقول المستوى + certificateDocument(ملف)
GET  /api/providers                           كل المزودين (بدون فلترة) — يشمل lat/lng محسوبة من آخر منتج للمزوّد له إحداثيات (فاضية لمزودي وايت ماء/سطحة)
POST /api/products                            body: providerId, name, description, size, price, minDays, city, neighborhood, lat, lng
GET  /api/products/provider/:providerId       منتجات مزود معيّن (لوحة المزود)
GET  /api/products/available?city=..          منتجات الحاوية المتاحة بمدينة معيّنة + بيانات المزود (JOIN) — يستخدمها تدفق العميل، يشمل provider_review_count
PUT  /api/products/:id                        تعديل (نفس الحقول + isAvailable)
DELETE /api/products/:id
POST /api/employees                           body: name, phone, iban, role, username, password — إضافة موظف بلوحة الإدارة
GET  /api/employees                           كل الموظفين (بدون حقل password)
PUT  /api/employees/:id                       body: نفس حقول الإضافة + isActive — password اختياري (فاضي = تبقى بدون تغيير)
DELETE /api/employees/:id
POST /api/employees/login                     body: { username, password } — يرفض لو is_active = 0
GET  /api/cities                              كل المدن (للوحة الإدارة، تشمل غير النشطة)
GET  /api/cities/active                       المدن النشطة فقط — يستخدمها container-location.js
POST /api/cities                              body: { name } — يرفض لو الاسم مكرر
PUT  /api/cities/:id                          body: { name, isActive }
DELETE /api/cities/:id
GET  /api/coupons                             كل الكوبونات (بلوحة الإدارة)
POST /api/coupons                             body: code, discountType (percent/fixed), discountValue, maxDiscount, usageLimit, expiresAt — code يُحفظ UPPERCASE دايماً
PUT  /api/coupons/:id                         نفس حقول الإضافة + isActive
DELETE /api/coupons/:id
POST /api/coupons/validate                    body: { code, amount } — معاينة فقط، بدون احتساب استخدام؛ يرجّع discount/discountType/discountValue/maxDiscount
GET  /api/reports/commissions                 عمولات اليوم/الأسبوع/الشهر/السنة/الإجمالي + تفصيل حسب كل خدمة (من بيانات orders الفعلية)
GET  /api/reports/payments                    سجل معاملات كل الطلبات: المبلغ، الخصم، عمولة المنصة، صافي المزوّد
GET  /api/operations/live                     إحصائيات لحظية (طلبات نشطة، مزودون متاحون، متوسط وقت قبول، إيرادات اليوم) + تنبيهات محسوبة — لمركز العمليات المباشر
GET  /api/services                            كل الخدمات (للوحة الإدارة، تشمل غير النشطة)
GET  /api/services/active                     الخدمات النشطة فقط، مرتبة حسب sort_order — يستخدمها app.js وorder.js
POST /api/services                            body: name, icon, description, badgeType, actionType, minPrice, timeEstimate, sortOrder — يرفض لو الاسم مكرر
PUT  /api/services/:id                        نفس حقول الإضافة + isActive
DELETE /api/services/:id
GET  /api/chats                               كل المحادثات مجمّعة حسب order_id + آخر رسالة/مرسل + عداد غير مقروء + needs_intervention محسوبة
GET  /api/chats/:orderId                      كل رسائل محادثة طلب محدد، مرتبة زمنياً
POST /api/chats                               body: { orderId, sender, senderPhone, message }
PUT  /api/chats/:orderId/read                 يصفّر is_read لكل رسائل الطلب (يستدعيه panel المحادثة بلوحة الإدارة تلقائياً عند الفتح)
POST /api/notifications/send                  body: { title, message, type, target, targetPhone } — ينشئ صف مستقل لكل مستلم مطابق للوجهة
GET  /api/notifications/:phone                إشعارات مستخدم محدد (عميل أو مزوّد) — تستخدمها جرس index.html وقسم إشعاراتي بـ profile.html
PUT  /api/notifications/:id/read              تعليم إشعار واحد كمقروء
PUT  /api/notifications/read-all/:phone       تعليم كل إشعارات مستخدم محدد كمقروءة
DELETE /api/notifications/:id                 حذف صف إشعار واحد (حذف "حدث إرسال" كامل بلوحة الإدارة = حذف كل صفوفه بالتوازي)
GET  /api/notifications/admin/all             كل صفوف الإشعارات بالنظام (مرسلة يدوياً أو تلقائية) — سجل موحّد بلوحة الإدارة
POST /api/complaints                          body: { orderId, reporterPhone, reporterType, reportedPhone, reportedType, type, description } — يطلق إشعار فوري للإدارة + تنبيه تكرار لو وصلت شكاوى reportedPhone لـ 3 بالضبط
GET  /api/complaints                          كل الشكاوى (JOIN مع orders/users/providers لاسم الخدمة والمُبلِّغ/المُبلَّغ عنه) — لوحة الإدارة
GET  /api/complaints/:id                      تفاصيل شكوى واحدة (نفس الـ JOIN)
GET  /api/complaints/user/:phone              شكاوى قدّمها مستخدم معيّن (reporter_phone)
PUT  /api/complaints/:id/status               body: { status, adminNote } — تغيير الحالة يدوياً بدون إجراء موثّق، يسجّل resolved_at أول مرة توصل resolved/closed
PUT  /api/complaints/:id/action               body: { actionType, note, duration, replyMessage } — endpoint واحد لكل الأزرار السبعة (resolve/refund/warn/suspend/delete/reply/close)، يضيف سطر بـaction_taken التراكمي
POST /api/reviews                             body: { orderId, reviewerPhone, reviewerType, reviewedPhone, reviewedType, rating, comment } — يرفض تقييم مكرر لنفس الطلب من نفس المُقيِّم، يعيد حساب rating المزوّد فوراً
GET  /api/reviews                             كل التقييمات (JOIN مع orders/users/providers) — لوحة الإدارة
GET  /api/reviews/provider/:phone             كل تقييمات مزوّد معيّن (ظاهرة ومخفية) — تستخدمها لوحة المزوّد
GET  /api/reviews/stats                       متوسط عام، إجمالي، نسبة راضين/غير راضين، توزيع نجوم، أفضل/أسوأ 3 مزودين
PUT  /api/reviews/:id/visibility               body: { isVisible } — يعيد حساب rating المزوّد
PUT  /api/reviews/:id/reply                    body: { reply } — يُرسل إشعار حقيقي للمُقيِّم
PUT  /api/reviews/:id/flag                     body: { isFlagged } — يُرسل إشعار للإدارة عند التفعيل
DELETE /api/reviews/:id                       يعيد حساب rating المزوّد
```

**Backend:** Node.js + Express.js, better-sqlite3, CORS + dotenv, multer (رفع الملفات), bcryptjs (تشفير كلمات مرور الموظفين)

## هوية التصميم

```css
--black:  #111111  /* الأسود الرئيسي */
--yellow: #F5C518  /* الأصفر الذهبي */
--white:  #FFFFFF
--gray:   #FAFAFA  /* رمادي فاتح */
--green:  #10B981  /* عمليات ناجحة */
--red:    #EF4444  /* أخطاء وإلغاء */
```

مبادئ التصميم: Arabic First (RTL دائماً) — Mobile First — Minimal (بسيط ونظيف) — خط Cairo للنصوص العربية — بطاقات بزوايا مستديرة (`border-radius: 16px`) — ظل خفيف على البطاقات — بلا gradients، ألوان ثابتة فقط.

مكوّنات متكررة: `.service-row`, `.btn`, `.btn-small`, `.header`, `.footer`, `.app-sidebar`, `.how-card`, `.icon` (أيقونات SVG — راجع قسم "الأيقونات" أعلاه)

## ملاحظات مهمة للمطوّر

1. السيرفر محلياً على port 3000 (افتراضي) — على الاستضافة يقرأ المنفذ من متغير البيئة `PORT` (`server.js`: `process.env.PORT || 3000`)
2. قاعدة البيانات `yashjub.db` في نفس المجلد — محلية وغير دائمة على Render (تنمحي مع كل نشر جديد، راجع قسم "الاستضافة" أعلاه)
3. كلمة مرور الإدارة: `yashjub2025`
4. OTP الحالي محاكاة فقط — يظهر في alert (لم يُربط بمزود SMS حقيقي بعد)
5. العمولة 5% تُخصم تلقائياً
6. localStorage يحفظ `yashjub_phone` و `yashjub_type`
7. `.gitignore` يستثني: `node_modules/`, `yashjub.db`, `.env`, `.claude/`, `uploads/`
8. API URLs تستخدم `window.location.origin` لتعمل محلياً وعلى الإنترنت بدون تعديل
9. الجوال يحتاج الرابط الحقيقي (Render) — localhost لا يعمل على الجوال مباشرة

## تشغيل المشروع محلياً

```bash
cd Desktop/YASHJUB
node server.js
# افتح المتصفح على http://localhost:3000
```

## رفع التحديثات

```bash
git add .
git commit -m "وصف التغيير"
git push
# Render يحدّث تلقائياً
```

## الميزات المنجزة

OTP دخول (محاكاة)، تمييز عميل/مزوّد، نظام طلبات كامل، صفحات طلب مخصصة لكل خدمة، نظام حاوية مبني على كتالوج منتجات فعلي (اختيار موقع → اختيار منتج حقيقي من مزود → أيام+تاريخ)، نظام سطحة (موقعين)، تتبع الطلب بالمراحل، لوحة مزوّد (إحصائيات+محفظة+طلبات)، تسجيل مزوّد (3 مستويات)، إدارة منتجات مزود الحاوية (إضافة/تعديل/حذف)، خرائط Leaflet/OpenStreetMap لتحديد المواقع (طلب، تتبع، منتجات المزودين)، صفحة طلباتي، ملف شخصي، لوحة إدارة، إدارة موظفين بصلاحيات حسب المنصب (مدير عام/مشرف/دعم فني/مراجع توثيق/محاسب)، تقارير عمولات ومدفوعات حقيقية بلوحة الإدارة، إدارة مدن (تغذي خطوة اختيار موقع الحاوية)، كوبونات خصم فعلية (يطبّقها العميل بصفحة الطلب لأي خدمة)، مركز عمليات مباشر بلوحة الإدارة (خريطة حية + تنبيهات + تدخل يدوي لتعيين مزوّد + تحديث تلقائي كل 30 ثانية)، إدارة كتالوج الخدمات (يغذي قائمة الخدمات بالصفحة الرئيسية ديناميكياً)، نظام محادثات حقيقي بين العميل والمزوّد محفوظ بالسيرفر (بدل الشات المحلي القديم) مع مركز مراقبة وتدخل بلوحة الإدارة، نظام إشعارات كامل (إرسال يدوي جماعي/فردي من لوحة الإدارة + إشعارات تلقائية عند أحداث النظام + جرس بالصفحة الرئيسية + قائمة كاملة بالملف الشخصي)، نظام بلاغات وشكاوى كامل (تقديم من العميل والمزوّد + مراجعة وإجراءات إدارية حقيقية بلوحة الإدارة تشمل إيقاف/حذف حسابات فعلي)، نظام تقييمات كامل (تقييم العميل للمزوّد بعد اكتمال الطلب يحسب متوسط تقييم المزوّد الحقيقي تلقائياً بكل مكان يظهر فيه بالمنصة، مع لوحة مراقبة وإجراءات إدارية)، صفحات عن/تواصل/شروط/404، وضع ليلي/نهاري، Responsive، أيقونات SVG موحّدة بكل الموقع تتبع لون النص (أسود/أبيض حسب الوضع)، رفع على Render وGitHub.

## الميزات القادمة (غير مُنفَّذة بعد)

SMS حقيقي (Twilio)، بوابة دفع حقيقية (STC Pay)، دومين yashjub.sa، تطبيق جوال (iOS/Android)، إشعارات Push، نظام Escrow حقيقي، فواتير PDF، SMS إشعار للمزوّد عند طلب جديد، تخزين دائم لقاعدة البيانات والملفات المرفوعة (راجع قسم "الاستضافة").

**كل أقسام لوحة الإدارة صارت مُنفَّذة فعلياً** (مركز العمليات المباشر، الخدمات، المحادثات، الإشعارات، البلاغات والشكاوى، والتقييمات) — ماكو أي قسم "قريباً" متبقٍ بلوحة الإدارة. راجع الأقسام المخصصة لكل وحدة أعلاه.

## روابط مهمة

| الرابط | الوصف |
|---|---|
| `https://yashjub.onrender.com` | الموقع الحي |
| `https://github.com/YASHJUB/yashjub` | GitHub |
| `https://dashboard.render.com` | لوحة تحكم الاستضافة |
| `/admin.html` | لوحة الإدارة |
| `/provider.html` | لوحة المزوّد |
